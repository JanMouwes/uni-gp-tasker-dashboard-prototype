export default class Level {
    /**
     *
     * @param {string} title
     * @param {Task[]} tasks
     */
    constructor(title, tasks) {
        this.title = title;
        this.tasks = tasks;
    }
}